# ADR 0005: Provider and coding adapters

**Decision:** Provider/model access and code assistants are host-owned adapter contracts, not renderer integrations.

**Consequences:** The initial documented adapters are OpenAI Responses API and a local coding CLI; neither is activated in Phase 0–1.
