# Build and release records

[Local builds](local-builds.md) is the maintained guide for current development and production artifacts.

Files named `phase-*` are historical milestone checklists. Checked boxes record what was asserted and tested for that milestone; they do not replace current automated checks or the current release gate. Run `pnpm verify` for code validation and `pnpm release:check` for the complete repository gate.
